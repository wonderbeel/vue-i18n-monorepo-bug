{
  "name": "vue-i18n-monorepo-bug",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "preview": "turbo run preview",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^2.7.0",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
